# 模型规格：`模型 ID:thinking 级别`；级别可选 off / low / medium / high / max
llm:
  provider: opencode-go
  models:
    primary: deepseek-v4-flash:high
    fast: deepseek-v4-flash:off

# economy（省钱）| balanced（均衡）| quality（最高质量）
quality: balanced
